Issues & Insights Log
=====================
Olist BI Project — Running notes on data quality issues and analytical observations


────────────────────────────────────────────────────────────
ISSUE #1 — On-Time Delivery Rate shows ~100% for all regions
────────────────────────────────────────────────────────────
Date discovered: May 21, 2026
Where: Report A, Visual 2 (On-Time Rate by Seller Region)

Problem:
  All 5 seller regions show an on-time delivery rate of ~1.0 (100%),
  which produces no meaningful insight and is misleading.

Root cause:
  The ETL computes is_on_time = 1 if order_delivered_customer_date <= order_estimated_delivery_date,
  and is_on_time = 0 if delivered late, and NULL if the order was never delivered.
  The DAX measure excludes NULLs (undelivered orders) from the denominator.
  Since the Olist dataset only records a delivery date for orders that were
  successfully completed, the sample is heavily biased toward fulfilled orders,
  which naturally skew on-time. The undelivered/cancelled orders are silently
  excluded, inflating the rate to near 100%.

Decision:
  Replaced this visual with "Avg Review Score by Seller Region" which shows
  real variation across regions and is a genuinely useful business metric.

Recommendation for documentation (Section C / ETL notes):
  Note that is_on_time should be interpreted with caution — it reflects
  on-time performance only among delivered orders, not all orders placed.


────────────────────────────────────────────────────────────
INSIGHT #1 — Revenue & Gross Profit by Seller Tier
────────────────────────────────────────────────────────────
Date discovered: May 21, 2026
Where: Report A, Visual 1 (Revenue & Gross Profit by Seller Tier)

Business Purpose:
  This visual shows how total platform value is distributed across seller tiers.
  It helps identify whether Olist depends mainly on a small group of high-volume
  sellers or on a broad base of medium sellers.

Finding: Silver sellers drive the most total revenue

Observation:
  Silver tier sellers (5K–50K BRL individual revenue) collectively generate
  more total revenue (~8.1M BRL) than Gold sellers (~4.6M BRL) or Bronze (~3.2M BRL).

Interpretation:
  The "middle class" of sellers is the backbone of the platform.
  There are significantly more Silver sellers than Gold, so even though
  each Gold seller individually earns more, the volume of Silver sellers
  dominates the aggregate. This is consistent with the Pareto principle
  applied at the platform level.

Caveat:
  seller_tier is based on cumulative price sum (product revenue, freight excluded),
  not on gross profit or order frequency. A seller with one high-value transaction
  can reach Gold without being a consistently active or profitable seller.
  See Issue #2 below for full documentation of this limitation.


────────────────────────────────────────────────────────────
ISSUE #2 — seller_tier and gross_profit: documented limitations
────────────────────────────────────────────────────────────
Date discovered: May 21, 2026
Where: ETL (etl_load_dw.py), dim_seller and fact_order_item

Limitation A — seller_tier is a revenue bracket, not a performance measure
  Definition: Bronze < 5,000 BRL | Silver 5,000–50,000 BRL | Gold >= 50,000 BRL
  Basis: total_revenue = SUM(price) per seller — freight excluded, order count ignored.
  Problem 1: A seller with one expensive sale qualifies as Gold regardless of activity.
  Problem 2: The tier does not reflect margin — a high-revenue, low-margin seller
             ranks the same as a high-revenue, high-margin seller.
  Problem 3: total_revenue for tier uses price only; the revenue field in
             fact_order_item includes freight (price + freight_value). These are
             not on the same basis.

Limitation B — unit_cost is simulated, not from source data
  Definition: unit_cost = list_price * 0.60 (assumes 40% gross margin).
  list_price = avg(transaction price) per product across all orders in dim_product.
  Problem: unit_cost is derived from the product average price, not from the
           specific transaction price. This means gross_profit = price - unit_cost
           uses two different price references. In discounted transactions,
           unit_cost may exceed actual price * 0.60, understating profit.
           In extreme cases, gross_profit can be mathematically negative.

Limitation C — gross_profit excludes freight costs
  revenue = price + freight_value (includes freight as income).
  gross_profit = price - unit_cost (no freight cost deducted).
  These two metrics are not on a consistent basis; gross_profit overstates
  the true margin if freight is a real cost to the seller.

Justification for accepting these limitations (not fixing):
  1. The Olist dataset contains no actual cost data — simulation is the only option.
  2. The assignment explicitly permits "simulated datasets that reflect real-world
     entities and behaviors in a reliable manner" (BIA Project brief, Section B).
  3. The project is described throughout as a "prototype" — not a production
     financial system.
  4. The assignment encourages simplicity: "For simplicity – avoid SCD Type 2",
     "For simplicity – avoid Snowflake". The same principle applies here.
  5. Pre-calculated fact attributes (gross_profit, revenue) are explicitly required
     by the assignment: "if applicable, add pre-calculated fact attributes."

Required disclosure in report (Section C and Part II summary):
  - unit_cost is simulated as avg(transaction_price) * 0.60 per product (SCD Type 1).
  - gross_profit is an approximation and should not be interpreted as an accounting figure.
  - seller_tier reflects cumulative product revenue brackets; it is an operational
    segmentation variable, not a composite performance score.

Business implication:
  Retention and support programs should prioritize Silver sellers —
  losing a significant portion of them would hurt platform revenue more
  than losing a few Gold sellers.


────────────────────────────────────────────────────────────
INSIGHT #2 — Review Score vs Revenue per Seller (Scatter Chart)
────────────────────────────────────────────────────────────
Date discovered: May 22, 2026
Where: Report A, Visual 3 (Scatter Chart)

Business Purpose:
  This visual shows whether high-revenue sellers also deliver high customer
  satisfaction, or whether revenue and quality are decoupled on the platform.
  It helps identify which individual sellers are assets vs. risks to Olist.

Visual Design Note:
  A logarithmic scale is applied on the Y-axis (Sum of Revenue). This does NOT
  change the data values — a seller with 500K BRL revenue still plots at 500K.
  What changes is the axis spacing: each grid line represents a 10x step
  (0.1K → 1K → 10K → 100K → 1,000K) instead of equal intervals.
  This is necessary because seller revenues span 4 orders of magnitude.
  Without log scale, all Bronze sellers (low revenue) collapse invisibly to
  the bottom, making the chart useless. With log scale, all three tiers are
  clearly visible and comparable.

Findings:
  - Gold sellers (dark blue, large dots) cluster in the top-right — high revenue
    AND high review scores. These are the platform's most valuable sellers.
  - Some Gold sellers appear at review score ~3 — high revenue but mediocre
    satisfaction. These are platform risk sellers: commercially valuable but
    damaging Olist's reputation on partner platforms.
  - Bronze sellers (light blue, small dots) are scattered across the full
    review score range — low revenue, mixed quality. Some have excellent scores
    but haven't scaled. These are growth candidates.
  - Silver sellers (orange) occupy the middle band as expected.

Business implication:
  Olist should not rely on revenue alone to evaluate sellers. A seller in the
  high-revenue / low-review quadrant generates short-term GMV but risks
  degrading Olist's standing on Mercado Livre, B2W, and other partner platforms.
  Quality enforcement must be applied independently of revenue tier.


────────────────────────────────────────────────────────────
INSIGHT #3 — Monthly Revenue Trend by Seller Tier (Line Chart)
────────────────────────────────────────────────────────────
Date discovered: May 22, 2026
Where: Report A, Visual 5 (Line Chart, Page 2)

Business Purpose:
  This visual shows how revenue evolved over time across seller tiers,
  revealing platform growth trajectory, seasonal patterns, and whether
  each tier is expanding or plateauing.

Findings:
  - All three tiers show consistent growth from Oct 2016 through mid-2018,
    confirming the platform was in a healthy growth phase during this period.
  - Silver sellers consistently lead in revenue volume throughout the period.
  - A visible spike appears around November 2017, likely corresponding to
    Black Friday / seasonal peak demand.
  - Gold and Bronze lines track closely, suggesting similar growth rates
    for the top and bottom revenue brackets during this period.

Data Boundary Note (report disclosure required):
  The sharp decline at the end of the time series (Oct–Nov 2018) reflects
  an incomplete final period in the dataset rather than an actual revenue drop.
  The dataset covers orders through November 2018 with the last months only
  partially populated. This artifact should not be interpreted as a business trend.


────────────────────────────────────────────────────────────
ISSUE #3 — Scatter chart redesigned to a box plot (measurement-scale fix)
────────────────────────────────────────────────────────────
Date discovered: June 12, 2026
Where: Report A, Visual 3 (was "Average reviews over total revenue" scatter)

Problem (why the old design was formally wrong):
  The original visual was a SCATTER chart with:
    X = review_score (1-5), Y = revenue (BRL), one dot per seller.
  Per the course, a scatter "relationship" chart requires BOTH axes to be
  ratio-scale FACT variables. Revenue is ratio-scale (true zero, meaningful
  ratios), but review_score is ORDINAL — its order is meaningful but the gaps
  (3->4 vs 4->5) are not quantitatively equal. Plotting an ordinal variable on
  a continuous axis treats it as if it were quantitative, which is incorrect.
  The title also referred to the AVERAGE (mean) of review scores; mean is only
  valid for interval/ratio scales. Ordinal data should use median, mode, rank,
  percentiles, or a frequency/distribution view.

Decision:
  Replaced the scatter with a BOX-AND-WHISKER chart:
    X = review_score (ordinal dimension, categories 1-5)
    Y = revenue (ratio-scale fact)
    Color = seller_tier (Bronze/Silver/Gold)
  A box plot is course-correct: it compares the DISTRIBUTION of a ratio-scale
  fact variable across a nominal/ordinal dimension. No box-and-whisker visual
  ships natively in Power BI, so the certified "Box and Whisker chart" (MAQ
  Software) was imported via Get more visuals.

Old visual kept as a learning artifact: assets/report_a_v3_scatter.png


────────────────────────────────────────────────────────────
ISSUE #4 — Box plot showed a "(Blank)" category on the review_score axis
────────────────────────────────────────────────────────────
Date discovered: June 12, 2026
Where: Report A, Visual 3 (box plot)
Screenshot: assets/report_a_v3_blank_issue.png

Problem:
  The review_score axis displayed six categories: (Blank), 1, 2, 3, 4, 5.
  The (Blank) box represented order items with no review score.

Root cause:
  review_score is nullable (olist_dw_schema.sql: "review_score SMALLINT", no
  NOT NULL). In etl_load_dw.py (build_fact_order_item) reviews are attached
  with a LEFT join:
      fact = fact.merge(rev, on="order_id", how="left")
  The Olist source only has a review row for orders that were actually reviewed
  (~99,224 reviews vs 112,650 order items). Every order item belonging to an
  un-reviewed order therefore gets review_score = NULL. Power BI groups all
  NULLs into a single "(Blank)" category on the axis. It is NOT a data error —
  it correctly represents "items that never received a customer review."

Fix:
  Applied a VISUAL-LEVEL filter on review_score: unchecked "(Blank)" (kept
  1-5). Equivalent to an Advanced filter "review_score is not blank".

Why a visual filter and not an ETL change:
  The NULLs are legitimate (unreviewed items) and other visuals may need them.
  Filtering at the visual scope removes the noise from THIS chart only, is fully
  reversible, and does not alter the warehouse.


──────────────────────────────────────────────────────
ISSUE #5 — "Average Review Score" in Category Stats matrix (ordinal mean)
──────────────────────────────────────────────────────
Date discovered: June 12, 2026
Where: Report A, Category Stats matrix (column "Average Review Score")

Problem:
  The matrix reported the arithmetic MEAN of review_score per category/tier.
  review_score is ORDINAL: order is meaningful, but the gaps are not
  quantitatively equal (3->4 is not assumed equal to 4->5). The course allows
  mean only for interval/ratio scales; ordinal data should use median, mode,
  rank, percentiles, or frequency/distribution.

Fix:
  Replaced "Average Review Score" with a frequency-based ratio measure,
  % Positive Reviews (share of items scored 4 or 5):

    % Positive Reviews =
    DIVIDE(
        CALCULATE(COUNTROWS(fact_order_item), fact_order_item[review_score] >= 4),
        CALCULATE(COUNTROWS(fact_order_item), NOT ISBLANK(fact_order_item[review_score]))
    )

  This is course-valid for ordinal data (a frequency proportion), is expressed
  as a ratio (suitable as a KPI), and is robust to the unequal-gap problem.
  (Median Review Score = MEDIAN(fact_order_item[review_score]) was considered as
  an ordinal central-tendency alternative.)

  A mirror measure, % Negative Reviews (share of items scored 1 or 2), was added
  alongside it for a balanced satisfaction picture:

    % Negative Reviews =
    DIVIDE(
        CALCULATE(COUNTROWS(fact_order_item), fact_order_item[review_score] <= 2),
        CALCULATE(COUNTROWS(fact_order_item), NOT ISBLANK(fact_order_item[review_score]))
    )

  Score 3 is treated as neutral and excluded from both buckets, so
  % Positive + % Negative + neutral = 100%. Both are now columns in the matrix.

  Final-report insight (Final_First_report.png): % Positive ranges ~0.73
  (Home & Garden) to ~0.83 (Books & Media) while gross margin is nearly flat
  (~0.32-0.35), so satisfaction -- not margin -- is the real category
  differentiator. The box plot confirms median revenue rises monotonically
  with review score (score 5 has the highest, widest revenue distribution).


──────────────────────────────────────────────────────
CHANGE #1 — Item Category slicer replaced by matrix cross-filtering
──────────────────────────────────────────────────────
Date: June 12, 2026
Where: Report A

Change:
  Removed the standalone "Item Category" slicer. Category filtering is now driven
  by clicking a Product Category row in the Category Stats matrix (Edit
  interactions -> set the box plot interaction to Filter). This reduces visual
  clutter and makes the matrix the single analytical entry point for category
  drill-down.


──────────────────────────────────────────────────────
CHANGE #2 — Box plot Y-axis: revenue -> delivery_days; tier bar -> revenue pie
──────────────────────────────────────────────────────
Date: June 12, 2026
Where: Report A (final image = assets/Final_First_report.png)

Change:
  (a) Box plot reframed from "Revenue Distribution by Review Score" to
      "Delivery Days Distribution by Review Score". Both are valid (ratio-scale
      fact across an ordinal dimension), but delivery_days answers a tighter,
      controllable business question. Result: median delivery time drops
      monotonically ~19 days (score 1) -> ~10 days (score 5), making fulfilment
      speed the headline driver of satisfaction.
  (b) Tier visual changed from a revenue/gross-profit bar chart to a
      "Revenue by seller_tier" pie: Silver ~51% (8.11M), Gold ~29% (4.58M),
      Bronze ~20% (3.16M).

Honest caveats (for the defense):
  - Pie with 3 slices is acceptable for part-to-whole, but a bar chart reads more
    precisely; the pie prioritises the "Silver dominates" message.
  - Delivery -> satisfaction is a strong correlation, not proven causation
    (category/season confounders not controlled in this single view).


──────────────────────────────────────────────────────
INSIGHT #4 / CHANGE #3 — Split delivery time into seller vs carrier phases
──────────────────────────────────────────────────────
Date: June 12, 2026
Where: Schema (olist_dw_schema.sql), ETL (etl_load_dw.py), Report A

Motivation:
  Total delivery_days blends two ownership phases. Olist's model: the SELLER
  prepares + posts the package; a partner CARRIER collects + delivers it. To
  assign accountability for slow delivery we must separate the two.

Change (schema + ETL, fully reproducible re-run):
  Loaded order_delivered_carrier_date (the seller->carrier handoff) into the
  warehouse and added to fact_order_item:
    - sk_date_carrier         (handoff date FK to dim_date)
    - seller_handling_days = (carrier_handoff - purchase).days     [SELLER phase]
    - carrier_transit_days = (delivered - carrier_handoff).days    [CARRIER phase]
  (delivery_days unchanged = total purchase -> customer.)

Finding (avg days per review score, delivered+reviewed items):
    score | seller | carrier | total |    n
      1   |  4.2   |  14.5   | 19.2  | 12,436
      2   |  3.5   |  11.3   | 15.3  |  3,662
      3   |  3.1   |  10.0   | 13.6  |  9,178
      4   |  2.8   |   8.5   | 11.7  | 21,069
      5   |  2.4   |   7.3   | 10.2  | 63,023

  The CARRIER phase dominates both the absolute time (~3-4x the seller phase)
  and the variance tied to satisfaction: across 1*->5*, seller handling shrinks
  1.8 days while carrier transit shrinks 7.2 days. So ~80% of the delivery-time
  gap between angry and happy customers is carrier-side. The operational lever is
  logistics/last-mile coverage, not seller onboarding/SLAs. This also decomposes
  the delivery->satisfaction MECHANISM, strengthening the causal argument beyond
  a raw correlation (stratifiable, temporally ordered, operationally plausible).

Next visual (TODO): stacked/clustered box or bar of seller_handling_days vs
  carrier_transit_days across review scores to show the split visually.


──────────────────────────────────────────────────────
CHANGE #4 — Page redesign: pie + simulated margin OUT, delivery breakdown IN
──────────────────────────────────────────────────────
Date: June 12, 2026
Where: Report A (Power BI), README.md

What changed on the page:
  - Built "Delivery Time Breakdown by Category" stacked column:
      X = product_category_group
      Y (stacked) = Avg seller_handling_days + Avg carrier_transit_days
    The carrier (dark) segment dominates EVERY category bar -> the seller-vs-
    carrier accountability split holds platform-wide, not just in aggregate.
    Worst: Home & Garden / Electronics (~12.5 avg days). Best: Food & Drinks (~9.7).
  - REMOVED the "Revenue Share by Seller Tier" pie. It answered a different
    question (who earns) than the page's delivery story (who's accountable).
  - REMOVED the simulated "Gross Profit Margin %" matrix column. unit_cost is a
    fixed 40% assumption, so the column was noise, not signal. Better to drop it
    than to defend a fabricated number at the review.
  - REMOVED "% Negative Reviews" from the matrix to declutter; kept % Positive.
  - ADDED "Average of delivery_days" to the matrix. delivery_days is RATIO-scale,
    so a mean is statistically valid here (unlike review_score, which is ordinal).

Why it's better:
  The whole page now has ONE defensible theme grounded in real source data:
  delivery performance and who owns it. No mixed messaging, no simulated metrics.

Label nit to fix in PBIX: matrix column shows auto-name "%GT Count of sk_seller"
  and the breakdown legend shows "Average of seller_handling_days / carrier_transit_days".
  Rename in-visual (Seller Share % ; Seller handling / Carrier transit) for polish.
